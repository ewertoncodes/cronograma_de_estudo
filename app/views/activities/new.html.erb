<h2>Cadastrar Atividade</h2>
<br/>

<% if @activity.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@activity.errors.count, "error") %> 
    </h2>
    <ul>
      <% @activity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<br/>
<br/>

<%= form_for @activity, :html => {:class => 'form form-horizontal'} do |f| %>

  <div class="form-group">
    <label for="" class="col-sm-2 control-label"><%= t('activerecord.attributes.activity.name') + ':' %></label>
    <div class="col-sm-10">
      <%= f.text_field :name %>
    </div>
  </div>
  <div class="form-group">
    <label for="" class="col-sm-2 control-label"><%= f.label :period, "Período" %></label>
    <div class="col-sm-10">
      <%= f.text_field :period %>
    </div>
  </div>
  <div class="form-group">
    <label for="" ><%= f.label :description, "Descrição" %></label>
      <%= f.text_area :description, class: "formatting" %>
  </div>
  <%= button_tag(type: 'submit', class: "btn btn-primary") do %>
   <i class="icon-ok icon-white"></i> Cadastrar
  <% end %>
<% end %>

